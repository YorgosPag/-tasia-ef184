
'use client';
import React from 'react';

// This layout can be used for specific dashboard-only styles or providers in the future.
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
